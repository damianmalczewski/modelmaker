{
  "$modelmaker": "v1.0",
  "title": "com.example.dto.Blob",
  "description": "A type with byte[] fields: stored with defensive copies, serialized as base64.",
  "type": "object",
  "required": ["id", "payload"],
  "properties": {
    "id": { "type": "string" },
    "payload": { "type": "bytes" },
    "signature": { "type": "bytes" }
  }
}
