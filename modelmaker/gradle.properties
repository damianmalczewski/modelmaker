# Kover requires the Kotlin plugin to activate (it does not support pure-Java projects), but this
# module has no Kotlin sources. Suppress the Kotlin Gradle Plugin's automatic kotlin-stdlib
# dependency so it doesn't leak into this module's published POM.
kotlin.stdlib.default.dependency=false
